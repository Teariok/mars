name: victrix
description: Victrix Venus/Cerbo Dashboard
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  mqtt_client: ^9.8.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.0.5
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^1.1.1
  build_runner: ^2.3.3

flutter:
  uses-material-design: true
